{
  "name": "server",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "@types/multer": "^1.4.11",
    "azure-storage": "^2.10.7",
    "express": "^4.19.2",
    "jest": "^29.7.0",
    "multer": "^1.4.5-lts.1",
    "supertest": "^7.0.0"
  },
  "scripts": {
    "build": "tsc",
    "dev": "AZURE_URL=$(./bin/extract_connection_string.sh) ts-node src/index.ts",
    "start": "AZURE_URL=$(./bin/extract_connection_string.sh) node dist/index.js",
    "test": "AZURE_URL=$(./bin/extract_connection_string.sh) jest --testMatch '**/*.test.js'"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.3"
  }
}
